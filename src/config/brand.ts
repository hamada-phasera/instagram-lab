/**
 * Own brand name used as the `brand_name` argument in Claude catalog prompts.
 * Edit per project — the brand_translation field of the catalog analysis is
 * rendered in terms of this name.
 */

export const BRAND_NAME = "FRIJOLES";
